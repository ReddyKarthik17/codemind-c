#include<stdio.h>
int main()
{
    int i,n;
    scanf("%d",&n);
    {
        for (i=n;i<=n;i++)
        {
            if(n>0)
            printf("Positive Number");
            else
            printf("Negative Number");
        }
    }
}